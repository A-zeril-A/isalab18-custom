<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="1">
        <!-- Scheduled Action to Clean Up Orphaned Attachments for Business Trips -->
        <record id="ir_cron_cleanup_business_trip_attachments" model="ir.cron">
            <field name="name">Business Trip: Cleanup Orphaned Attachments</field>
            <field name="model_id" ref="model_business_trip_cleanup"/>
            <field name="state">code</field>
            <field name="code">model._cron_cleanup_orphaned_attachments()</field>
            <field name="user_id" ref="base.user_root"/>
            <field name="interval_number">1</field>
            <field name="interval_type">days</field>
            <field name="numbercall">-1</field>
            <field name="doall" eval="False"/>
            <field name="active" eval="True"/>
            <field name="nextcall" eval="(DateTime.now() + timedelta(days=1)).strftime('%Y-%m-%d 03:00:00')" />
        </record>

        <!-- Scheduled Action to Send Expense Submission Reminders -->
        <record id="ir_cron_send_expense_reminders" model="ir.cron">
            <field name="name">Business Trip: Send Expense Submission Reminders</field>
            <field name="model_id" ref="model_business_trip"/>
            <field name="state">code</field>
            <field name="code">model._cron_send_expense_submission_reminders()</field>
            <field name="user_id" ref="base.user_root"/>
            <field name="interval_number">1</field>
            <field name="interval_type">minutes</field>
            <field name="numbercall">-1</field>
            <field name="doall" eval="False"/>
            <field name="active" eval="True"/>
            <field name="nextcall" eval="(DateTime.now() + timedelta(minutes=1)).strftime('%Y-%m-%d %H:%M:%S')" />
        </record>
    </data>
</odoo> 